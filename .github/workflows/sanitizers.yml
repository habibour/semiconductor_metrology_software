name: Sanitizers

# CLAUDE.md 7 / 10: ThreadSanitizer and AddressSanitizer+UBSan are separate
# builds (thread and address cannot be combined) and must stay clean. They run
# on a GitHub macOS runner because macOS is the only supported platform (PRD
# D-11) and the sanitizer runtimes do not work on the newest macOS the
# developer's own machine runs.

on:
  push:
  pull_request:

jobs:
  address-undefined:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure (ASan + UBSan)
        run: >
          cmake --preset asan
          -DSSIM_SANITIZER=address,undefined
          -DSSIM_ENABLE_SECSGEM=ON

      - name: Build
        run: cmake --build build-asan -j

      - name: Test
        env:
          ASAN_OPTIONS: detect_leaks=0:strict_string_checks=1
          UBSAN_OPTIONS: print_stacktrace=1:halt_on_error=1
        run: ctest --test-dir build-asan --output-on-failure

  thread:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Configure (TSan)
        run: cmake --preset tsan -DSSIM_ENABLE_SECSGEM=ON

      - name: Build
        run: cmake --build build-tsan -j

      - name: Test
        env:
          TSAN_OPTIONS: halt_on_error=1:second_deadlock_stack=1
        run: ctest --test-dir build-tsan --output-on-failure
